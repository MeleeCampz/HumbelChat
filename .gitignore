# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
 eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
.venv/
venv/
env/
.env.local

# Environment files with secrets
.env
.env.*
!.env.example

# IDE
.idea/
.vscode/
*/.DS_Store

# OS
.DS_Store
Thumbs.db

# Logs and temp output files
*.log
bot.log
chat_response.txt
nohup.out
*.tmp
*_temp.json

# Dependencies (if vendored)
dependencies/
vendor/

# Secrets / credentials (belt + suspenders)
*.pem
*.key
*.jwk
credentials/
secrets/

# Private working files — keep locally, do not expose on GitHub  
ISSUES_AND_TODOS.md
TODOs.txt
characters.json

# AI model artifacts - local only, may contain private data
.qwen/

# Local AI working tasks — for development-only notes, not tracked
todo/

# Data folder - keep locally but don't track in git
data/

# Git
.git/