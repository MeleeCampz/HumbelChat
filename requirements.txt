# Pinned for reproducibility (code review §3.10).
# Versions reflect the working environment verified 2026-08-22.
python-dotenv==1.2.2
# [voice] extra pulls in PyNaCl (transport encryption) + davey (DAVE E2EE, mandatory
# for all Discord voice since 2026-03-01). libopus is loaded by discord.py at runtime
# via ctypes — install the system package if it's missing (e.g. `apt-get install libopus0`)
discord.py[voice]==2.7.1
cryptography==49.0.0   # AES-256-GCM voice transport decryption (also a transitive dep)
openai==2.44.0
httpx==0.28.1
aiohttp==3.14.1
numpy==1.26.4
faster-whisper==1.2.1   # local STT backend (STT_BACKEND=local): segment timestamps -> interleaved transcript
fastembed==0.8.0   # optional — enable vector retrieval (RAG_RETRIEVAL_METHOD=vector)

# Test dependencies
pytest==9.1.1
pytest-asyncio==1.4.0
